import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
    },
    charImageContainer: {
        width: 200,
        height: 300,
        borderRadius: 5,
        overflow: 'hidden',
        marginBottom: 40,
        borderWidth: 2,
        marginTop: 70,
    },
    charImage: {
        width: '100%',
        height: '100%',
    },
    infoContainer: {
        alignItems: 'center',
    },
});

export default styles;
